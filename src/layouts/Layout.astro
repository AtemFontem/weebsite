---
import Header from "../components/Header.astro";

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<script>
  history.scrollRestoration = "manual";
</script>

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="photo archive" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="png" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/favicon-iphone.png" />
    <meta name="generator" content={Astro.generator} />
    <meta
      name="google-site-verification"
      content="DFOj9EFSqoA6HGAdofIsI28eL1u38xj9DM4TxCl1XEg"
    />
    <title>{title ? title : "aymenis.online"}</title>
  </head>
  <body>
    <Header />
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --background: #fffcf8;
    --text: #3c4f34;
    --link: #57a348;
  }

  /* Dark mode */
  @media (prefers-color-scheme: dark) {
    :root {
      --background: #3c4f34;
      --text: #fffcf8;
      --link: #D3EEB4;
    }
  }

  @font-face {
    font-family: bold;
    src: url(/IBMPlexMono-Bold.ttf);
  }

  @font-face {
    font-family: semibold;
    src: url(/IBMPlexMono-SemiBold.ttf);
  }

  html {
    height: 100%;
    background-color: var(--background);
    color: var(--text);
    scroll-behavior: smooth;
  }

  a {
    color: var(--link);
    text-decoration: none;
  }

  a:hover {
    text-decoration: underline;
  }

  h1 {
    font-size: 32px;
  }

  h2 {
    font-size: 24px;
  }

  h3 {
    font-size: 18px;
  }

  body {
    font-family: semibold;
    font-size: 16px;
  }

  h1,
  h2,
  h3 {
    font-family: bold;
  }

  body {
    display: flex;
    flex-direction: column;
    padding: 12px;
    gap: 24px;
  }

  ul {
    list-style-type: none;
  }
</style>
