---
// this only runs when the site is built
// github pages servers always set the date a day ahead of when i push
// timezones are evil
const day = 24 * 60 * 60 * 1000;
const today = new Date(Date.now() - day);

const formatDate = (date: Date) => {
  const year = date.getFullYear();
  const month =
    date.getMonth() + 1 > 9
      ? `${date.getMonth() + 1}`
      : `0${date.getMonth() + 1}`;
  const day = date.getDate() > 9 ? `${date.getDate()}` : `0${date.getDate()}`;

  return `${year}-${month}-${day}`;
};
---

<header>
  <h1 id="top"><a href="/" id="title" }>full wash and set</a></h1>
  <p>last updated: {formatDate(today)}</p>
</header>

<style>
  header {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  h1 {
    margin-bottom: 0;
  }

  p {
    margin: 0;
  }

  @media only screen and (max-width: 700px) {
    header {
      text-align: center;
    }
  }
</style>
